This is a password generator that uses the Klingon dictionary.  
The goal was to generate a password only using full words, 
without truncating the words within the desired length, including the numbers.
